ACLOCAL_AMFLAGS = ${ACLOCAL_FLAGS} -I m4

SUBDIRS =
if BUILD_ACCTONBF_PLAT
SUBDIRS += include accton-bf
endif
if BUILD_NEWPORT_PLAT
SUBDIRS += include newport
endif
if BUILD_TOFINO_BRINGUP_PLAT
SUBDIRS += include tofino-bringup
endif
